#include <iostream>
using namespace std;

int main()
{
    int n;
    cout<<"enter the number: ";
    cin >> n;

    cout << (n % 2 == 0 ? "number is Even" : "number is odd");

    return 0;


}